## Project Documentation Continuity

> Merge this routing into the target project's existing agent-instruction or
> governance entry. Replace every placeholder and remove this note.

- Read `<responsibility map or combined project document>` before making
  project-level claims.
- Use its canonical write routing; do not duplicate durable facts in this
  instruction entry.
- Recheck documentation impact after a durable contract, architecture,
  operational, implementation, verification, priority, pause, or recovery
  event: trace changed facts to their owners and bounded actual consumers,
  including files outside the diff.
- Update an existing canonical owner with its recorded update mode when the
  route remains valid. Apply modes by content or section: keep the current
  snapshot coherent, revise approved contracts, preserve frozen history and
  point to later dispositions, and update derived views at their true source.
- Check duplicate current facts even within one file; retain necessary history
  and evidence/recovery pointers instead of accumulating current narratives.
- Verify targets, writer, and applicable authorization before writing; reuse
  valid approval for its scope rather than asking again for each update.
- Creating, splitting, merging, renaming, migrating, or changing authority or
  canonical ownership needs explicit authorization for those effects. Reuse it
  when already given; otherwise ask before acting.
- When routing is broken or a new module, structural change, or authority
  choice is needed outside existing authorization, stop the affected mutation
  and visibly propose a Project Docs repair. Complete an already-authorized
  repair within its concrete effects. This routing line is neither Skill
  invocation nor authorization for the change.
- Stop before writing when permission, scope, writer ownership, or evidence is
  unresolved.
- Resume interrupted work from `<recovery entry>`.
