interface:
  display_name: "Project Docs"
  short_description: "Repair and sustain reliable project truth"
  default_prompt: "Use $manage-project-docs to assess this project's documentation needs and update modes, follow my request and existing authorization, keep read-only work read-only, and complete authorized changes without repeating approval questions."

policy:
  allow_implicit_invocation: true
